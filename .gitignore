# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage
/playwright-report
/test-results
/playwright/.cache

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
# Vercel CLI env pull outputs — peuvent contenir des tokens OIDC + secrets
.env.supabase
.env.b2b-audit
.env.production
.env.preview
.env.development

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel

# Lighthouse / a11y test residues (local artefacts only)
tmp-lh/
tmp-a11y/
tmp-*/
.lh-audits/

# Phase 3 POC — fiches crypto generees auto (output)
content/cryptos-fiches/
!content/cryptos-fiches/.gitkeep

# Local-only audit & backup artefacts (audit Kev 19/05/2026).
# audit-output/ contient des rapports markdown internes parfois lourds
# (30 MB+) et stratégiques — pas user-facing, jamais à pousser sur main.
# backup/ contient des snapshots locaux (DNS, sitemap prod, etc.).
# *.bak* : rune Kev systématique avant édition prod (cf. CLAUDE.md).
audit-output/
backup/
*.bak
*.bak-*
.env.vercel-current

# Logs Codex/Hermes locaux
.codex-next-start.*.log
.next_vercel.log
